# 钢丝绳在线检测系统 — 用户使用手册

> 版本：V5.1.250828  
> 适用对象：现场操作员、质检人员（无需编程基础）  
> 下载页：https://dushengyao.github.io/mg4-release/

---

## 一、电脑要求

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10 或 Windows 11（64 位） |
| 网络 | 电脑与下位机在同一局域网（网线或 Wi‑Fi 均可） |
| 其他 | **不用**单独安装 .NET，安装包已自带运行环境 |

---

## 二、安装软件（只需做一次）

1. 打开下载页：https://dushengyao.github.io/mg4-release/
2. 点击 **「下载 Windows 安装包（中文向导）」**。
3. 得到文件：`mg4Setup-5.1.250828-x64.exe`。
4. **双击**该文件，安装向导为 **简体中文**（Inno Setup 中文界面）。
5. 按向导提示：
   - 选择 **「完整安装」**；
   - 可选勾选 **「创建桌面快捷方式」**；
   - 安装位置一般保持默认：  
     `C:\Users\你的用户名\AppData\Local\Programs\mg4\`
6. 安装完成后，桌面或开始菜单会出现 **「钢丝绳在线检测系统」** 图标。

> 若 Windows 提示「未知发布者」，点 **「仍要运行」** 或 **「更多信息」→「仍要运行」**（请从官方下载页获取安装包）。

---

## 三、第一次打开软件

### 3.1 登录窗口

1. 双击 **钢丝绳在线检测系统** 启动。
2. 出现 **「弱磁钢丝绳检测」** 登录框。
3. 输入 **启动密码**（由设备供应商提供，与下位机通讯密码不是同一个）。
4. 点 **「进入」**；不想用时点 **「取消」** 退出。

### 3.2 通讯配置（仅在下位机密码未配置时出现）

若软件检测到尚未设置下位机通讯密码，会弹出 **「通讯配置」** 窗口：

| 填写项 | 说明 | 常见默认值 |
|--------|------|------------|
| 下位机通讯密码 | 必须与下位机参数一致 | **1234** |
| 监听端口 | 电脑等待下位机连接的端口 | **9005** |

1. 一般 **直接保持默认**（1234 / 9005），点 **「保存并连接」**。
2. 若下位机改过密码或端口，按实际填写后再保存。
3. 点 **「稍后」** 可先进主界面，但 **无法联机**；之后可在 **「通讯连接」→「通讯设置」** 里再配。

> 正常新装用户：安装包已内置默认密码 **1234**，多数情况下 **不会** 再弹出此窗口。

---

## 四、与下位机联机

### 4.1 准备工作

1. 下位机已上电，网络已接好。
2. 记下本机 **局域网 IPv4 地址**（Windows「设置 → 网络和 Internet → 属性」）。

### 4.2 在软件里连接

1. 主界面左侧点 **「通讯连接」**。
2. 确认 **展示 IP** 为本机局域网 IP（不对就改成实际 IP）。
3. 确认 **监听端口** 为 **9005**（与下位机一致）。
4. 软件启动后会 **自动监听**；也可点 **「重新连接」**。
5. 下位机连上后：
   - 顶栏与遥测区圆点变为 **绿色**；
   - 状态由 **「未连接」** 变为 **「已连接 · IP:端口」**。

> **V5.0.250706 起**：断线后状态会在约 1 秒内同步为「未连接」，避免顶栏仍显示已连接却无法操作。

### 4.3 修改通讯密码或端口

1. 左侧 **「通讯连接」** → **「通讯设置」**。
2. 修改后点 **「保存并连接」**，软件会自动重新连接。

---

## 五、日常检测流程（推荐顺序）

```
联机 → 参数下发 → 启动检测 → （可选）开启实时流 → 停止检测 → 检伤结果查看
```

### 5.1 参数下发

1. 左侧 **「绳型参数」** 核对 **传感器数、采样步长、序列号** 等。
2. 联机状态下点 **「参数下发」**，等待下位机确认。
3. **MRT 设置**（左侧 **「Mrt设置」**）中仅配置 **一根钢丝绳** 参数即可（本版已简化为单绳设备）。

### 5.2 启动 / 停止检测

1. 在 **「监测总览」** 控制区。
2. 点 **「启动检测」**（需已联机；若连接已断开会提示 **「下位机未连接」**，程序不会崩溃）。
3. 需要在线看波形时：先 **启动检测**，再点 **「开启实时流」**。
4. 检测结束后点 **「停止检测」**；原始数据（0F）会自动保存到 **data** 目录。

### 5.3 查看检伤结果

1. 检测并保存完成后，点 **「检伤结果查看」**。
2. 主界面曲线切换为 **「检测报告 · 滤波后分通道曲线」**：
   - Y 轴为 ADC 刻度（0～约 2000，满量程 4080）；
   - 四通道曲线 **同一刻度**（不再错开 +500）；
   - 损伤位置在曲线 **顶部** 以 **橙色/黄色倒三角** 标记。
3. 无损伤时提示「本次检测没有损伤」。
4. 也可在 **「数据查询」** 里按日期查找历史记录。

### 5.4 实时曲线选项

- 勾选 **「分通道曲线」**：实时监测时四路波形纵向错开，便于区分（**仅实时曲线**；报告图固定同刻度）。

---

## 六、数据查询与报警

| 功能 | 入口 | 说明 |
|------|------|------|
| 损伤列表 | 数据查询 → 损伤列表 | 显示 **A～F 全等级** 检伤结果 |
| 报警记录 | 数据查询 → 报警记录 | 显示 **C 级及以上** 需关注报警 |
| 检测曲线 | 数据查询 → 检测曲线 | 选中记录后预览曲线 |
| 波形查看 | 检测曲线页 → **波形查看** | 打开深度波形窗口 |
| 重新检伤 | 数据查询 / 主界面 | 对已有 DAT 重新 Wire 分析 |

> **说明**：A/B 级（严重/超限）会出现在 **损伤列表**；**报警记录** 按规程默认只保留 **C 级及以上**（较重及以上需日常关注）。运维扩展会话下可查看更完整记录。

---

## 七、检测报告（Word）

1. 左侧 **「检测报告」** 打开报告生成窗口。
2. 选择检测记录，可生成 **Word 格式** 报告并在软件内 **本地预览**（需 Windows 自带 WebView2 运行库，Win10/11 通常已具备）。

---

## 八、常用功能速查

| 我想… | 怎么做 |
|--------|--------|
| 查历史检测记录 | 左侧 **「数据查询」** |
| 看报告曲线 | **「检伤结果查看」** 或 数据查询 → 检测曲线 |
| 看原始深度波形 | 数据查询 → 检测曲线 → **「波形查看」** |
| 重新做一次检伤分析 | **「重新检伤」** |
| 打开数据文件夹 | **「数据管理」** |
| 看连接日志 | 左侧 **「传输日志」** |
| 程序报错排查 | 左侧 **「查看异常日志」** |
| 改 MRT / 报废上限 | **「Mrt设置」** → 算法参数中的 **报废上限**（默认 10%） |

---

## 九、常见问题

### Q1：顶栏显示「已连接」但操作提示「下位机未连接」

- 请升级到 **V5.0.250706** 或更新版本；本版已修复状态不同步问题。
- 在 **「通讯连接」** 点 **「重新连接」**，确认下位机已连上且圆点为绿色。

### Q2：点击「启动检测」程序崩溃

- **V5.0.250706** 已修复：连接断开时仅提示错误，不会退出程序。
- 请先确认联机状态再启动检测。

### Q3：报警记录为空，但损伤列表有 A/B

- **属正常设计**：报警记录默认只显示 **C 级及以上**。
- A/B 请在 **损伤列表** 中查看；重新检伤后 C+ 会写入报警记录。

### Q4：报告曲线 Y 轴数值很大（几千）或有假尖峰

- **V5.0.250706** 已修复报告图刻度与标记方式。
- 请点 **「检伤结果查看」** 或 **「重新检伤」** 刷新报告曲线。

### Q5：提示「未连接」

- 检查网线/Wi‑Fi、电脑 IP 是否与下位机同网段。
- 检查 **通讯密码、端口**（默认 1234 / 9005）。
- 操作顺序：软件 **先监听** → 下位机 **再连入**。

### Q6：没有实时曲线

- 须 **先启动检测，再开启实时流**（只启动检测不会出现 0x0D 实时数据）。

### Q7：如何卸载

- **设置 → 应用 → 已安装的应用** → 找到 **钢丝绳在线检测系统** → 卸载；  
  或 **控制面板 → 程序和功能** → 卸载。

---

## 十、配置文件位置（供维护人员参考）

| 文件 | 路径（默认安装） |
|------|------------------|
| 主程序 | `%LocalAppData%\Programs\mg4\mg4.WpfUi.exe` |
| 通讯配置 | 同上目录下的 `setting.json` |
| 检测数据 | 同上目录下的 `data\` 文件夹 |
| 运行日志 | 同上目录下的 `Logs\` 文件夹 |
| MRT 参数 | 同上目录下的 `mrt_settings.json` |

> 普通用户 **无需** 手动编辑 `setting.json`，请优先使用软件内 **「通讯设置」**。

---

## 十一、本版更新摘要（V5.1.250828）

- 本机网关：确认后加入现场网段 / 恢复以太网临时地址。
- 启动检测自动打开实时流，停止检测增加确认。
- 首页损伤展示与设备报警对齐。
- 数据查询 / 数据管理改为右侧筛选。
- 连接状态与真实 TCP 同步；断线时启动/停止不再崩溃。
- **中文安装向导**（Inno Setup 简体中文版）。

---

## 十二、技术支持

遇到无法自行解决的问题，请向设备供应商提供：

1. 软件版本（主界面顶栏 **版本:V5.1.250828**）。
2. 问题发生时的截图。
3. **异常日志** 中最新记录（如有）。
4. **传输日志** 中与问题相关的片段（联机/检伤问题）。

---

*本手册随安装包发布，如有更新以官网下载页为准。*
