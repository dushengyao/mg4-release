# 钢丝绳在线检测系统 — 用户使用手册

> 版本：V5.0.250704  
> 适用对象：现场操作员、质检人员（无需编程基础）

---

## 一、电脑要求

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10 或 Windows 11（64 位） |
| 网络 | 电脑与下位机在同一局域网（网线或 Wi‑Fi 均可） |
| 其他 | **不用**单独安装 .NET，安装包已自带运行环境 |

---

## 二、安装软件（只需做一次）

1. 打开下载页面，点击 **「下载 Windows 版」**。
2. 得到文件：`mg4Setup-5.0.250704-x64.exe`。
3. **双击**该文件，按安装向导点「下一步」。
4. 安装位置一般保持默认即可，例如：  
   `C:\Users\你的用户名\AppData\Local\Programs\mg4\`
5. 安装完成后，桌面或开始菜单会出现 **mg4** 图标。

> 若 Windows 提示「未知发布者」，点 **「仍要运行」** 或 **「更多信息」→「仍要运行」**（需从官方下载页获取安装包）。

---

## 三、第一次打开软件

### 3.1 登录窗口

1. 双击 **mg4** 启动。
2. 出现 **「弱磁钢丝绳检测」** 登录框。
3. 输入 **启动密码**（由设备供应商提供，与下位机通讯密码不是同一个）。
4. 点 **「进入」**；不想用时点 **「取消」** 退出。

### 3.2 通讯配置（仅在下位机密码未配置时出现）

若软件检测到尚未设置下位机通讯密码，会弹出 **「通讯配置」** 窗口：

| 填写项 | 说明 | 常见默认值 |
|--------|------|------------|
| 下位机通讯密码 | 必须与下位机参数一致 | **1234** |
| 监听端口 | 电脑等待下位机连接的端口 | **9005** |

1. 一般 **直接保持默认**（1234 / 9005），点 **「保存并连接」**。
2. 若下位机改过密码或端口，按实际填写后再保存。
3. 点 **「稍后」** 可先进主界面，但 **无法联机**；之后可在 **「通讯连接」→「通讯设置」** 里再配。

> 正常新装用户：安装包已内置默认密码 **1234**，多数情况下 **不会** 再弹出此窗口。

---

## 四、与下位机联机

### 4.1 准备工作

1. 下位机已上电，网络已接好。
2. 记下下位机要连接的 **电脑 IP**（可在 Windows「设置 → 网络和 Internet → 属性」里查看 IPv4 地址）。

### 4.2 在软件里连接

1. 主界面左侧点 **「通讯连接」**。
2. 确认 **展示 IP** 为本机局域网 IP（不对就改成实际 IP）。
3. 确认 **监听端口** 为 **9005**（与下位机一致）。
4. 软件启动后会 **自动监听**；也可点 **「重新连接」**。
5. 下位机连上后，顶部状态由 **「未连接」** 变为 **「已连接」**，并显示客户端地址。

### 4.3 修改通讯密码或端口

1. 左侧 **「通讯连接」** → **「通讯设置」**。
2. 修改后点 **「保存并连接」**，软件会自动重新连接。

---

## 五、日常检测流程（推荐顺序）

```
联机 → 参数下发 → 开始检测 → 停止检测 → 查看检伤结果
```

### 5.1 参数下发

1. 左侧 **「绳型 / 采集参数」**（或「参数」相关菜单）。
2. 核对 **传感器数、采样步长、序列号** 等。
3. 联机状态下点 **「参数下发」**，等待下位机确认。

### 5.2 开始 / 停止检测

1. 在 **「仪表盘」** 或主控制区。
2. 点 **「开始检测」**（需已联机）。
3. 检测过程中可查看 **实时波形**。
4. 结束后点 **「停止检测」**；数据会自动保存到软件 **data** 目录。

### 5.3 查看检伤结果

1. 检测并保存完成后，点 **「检伤结果查看」**。
2. 有损伤：显示损伤等级统计与报告曲线。
3. 无损伤：提示「本次检测没有损伤」。
4. 也可在 **「数据查询」** 里按日期查找历史记录，点 **「波形查看」** 看原始曲线。

---

## 六、常用功能速查

| 我想… | 怎么做 |
|--------|--------|
| 查历史检测记录 | 左侧 **「数据查询」** |
| 看某条记录的波形 | 数据查询 → 选中记录 → **「波形查看」** |
| 重新做一次检伤分析 | 有数据后点 **「重新检伤」** |
| 打开数据文件夹 | **「数据管理」** 或相关「打开文件夹」按钮 |
| 看连接日志 | 左侧 **「传输日志」** |
| 程序报错排查 | 左侧 **「异常日志」** → 打开日志查看 |

---

## 七、常见问题

### Q1：提示「未连接」

- 检查网线/Wi‑Fi、电脑 IP 是否与下位机同网段。
- 检查 **通讯密码、端口** 是否与下位机一致（默认 1234 / 9005）。
- 在 **「通讯连接」** 点 **「重新连接」**。

### Q2：安装后一打开就提示找不到 setting.json

- 请 **重新安装** 最新版安装包，不要只复制单个 exe。
- 安装目录下应有 `setting.json` 文件。

### Q3：检伤结果一直是「待执行检伤分析」

- 等待几秒（后台自动分析）；或点 **「重新检伤」**。
- 确认检测已正常停止并保存了数据文件。

### Q4：如何卸载

- **设置 → 应用 → 已安装的应用** → 找到 mg4 → 卸载；  
  或 **控制面板 → 程序和功能** → 卸载。

---

## 八、配置文件位置（供维护人员参考）

| 文件 | 路径（默认安装） |
|------|------------------|
| 主程序 | `%LocalAppData%\Programs\mg4\mg4.exe` |
| 通讯配置 | 同上目录下的 `setting.json` |
| 检测数据 | 同上目录下的 `data\` 文件夹 |
| 运行日志 | 同上目录下的 `Logs\` 文件夹 |

> 普通用户 **无需** 手动编辑 `setting.json`，请优先使用软件内 **「通讯设置」**。

---

## 九、技术支持

遇到无法自行解决的问题，请向设备供应商提供：

1. 软件版本（主界面顶栏 **版本:V5.0.250704**）。
2. 问题发生时的截图。
3. **异常日志** 中最新记录（如有）。

---

*本手册随安装包发布，如有更新以官网下载页为准。*
